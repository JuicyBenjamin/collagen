{
  "name": "@collagen/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:dev": "prisma dev",
    "contract:emit": "prisma-next contract emit",
    "db:init": "prisma-next db init",
    "db:update": "prisma-next db update",
    "db:sign": "prisma-next db sign",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo node_modules"
  },
  "dependencies": {
    "@prisma-next/postgres": "^0.13.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@collagen/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "bun-types": "catalog:",
    "prisma": "^7.8.0",
    "prisma-next": "^0.13.0",
    "typescript": "catalog:"
  }
}
