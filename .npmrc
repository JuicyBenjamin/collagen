# Strict but allow workspace packages to resolve peers from the root.
auto-install-peers=true
strict-peer-dependencies=false
# Keep the store deterministic across the monorepo.
link-workspace-packages=true
prefer-workspace-packages=true
